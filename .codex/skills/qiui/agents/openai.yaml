interface:
  display_name: "QIUI"
  short_description: "代跑 qiui：电击/上锁/扫写"
  default_prompt: "按 $qiui 当操作员：用户要电击/上锁/控设备时，用 shell 直接执行 qiui（devices→run→scan/write），不要只拼命令或发 SAMPLE 教程。缺凭证/toyUid/MAC 再问一句然后继续跑。"
policy:
  allow_implicit_invocation: true
